# NPM Configuration for Pinpoint Travel App

# Engine strict mode - fail if Node/npm versions don't match engines field
engine-strict=true

# Use package-lock.json (prevent changes to lock file format)
lockfile-version=3
